global:
  scrape_interval: 60s
  evaluation_interval: 60s

scrape_configs:
  - job_name: rerb-api
    static_configs:
      - targets:
          - api:8000

  - job_name: cadvisor
    static_configs:
      - targets:
          - cadvisor:8080
